/**
 * Options accepted by the `lint` executor (none — the analyser is configured
 * by the workspace-root `analysis_options.yaml` each project includes).
 *
 * @typeParam None - this interface has no generic type parameters.
 */
export type LintExecutorSchema = Record<string, never>
