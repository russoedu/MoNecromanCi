/**
 * Options accepted by the `test` executor (none — `flutter test` discovers
 * the project's `test/` directory itself).
 *
 * @typeParam None - this interface has no generic type parameters.
 */
export type TestExecutorSchema = Record<string, never>
