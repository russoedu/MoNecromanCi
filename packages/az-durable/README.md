# az-durable

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build az-durable` to build the library.

## Running unit tests

Run `nx test az-durable` to execute the unit tests via [Jest](https://jestjs.io).
