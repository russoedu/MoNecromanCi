/**
 * Options accepted by the `lint` executor (none — ruff needs no configuration
 * beyond running in the project's own directory).
 *
 * @typeParam None - this interface has no generic type parameters.
 */
export type LintExecutorSchema = Record<string, never>
